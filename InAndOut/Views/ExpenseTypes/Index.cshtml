@model IEnumerable<ExpenseType>

<h1>Index - Expense Types</h1>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Expenses Types List</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="ExpenseTypes" asp-action="Create" class="btn btn-primary">Create New Expense Type</a>
        </div>
    </div>
    @{ if (Model.Count() > 0)
        {
            <table class="table table-bordered table-striped" style="width:100%">
                <thead>
                        <tr>
                            <th scope="col">Expense Type Name</th>
                            <th scope="col">Actions</th>
                        </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td scope="row" width="70%">@item.Name</td>
                            <td scope="row" width="30%">
                                <div class="w-100 btn-group" role="group">
                                    <a asp-controller="ExpenseTypes" asp-action="Update" asp-route-Id="@item.Id" class="btn btn-primary mx-1">Update</a>
                                    <a asp-controller="ExpenseTypes" asp-action="Delete" asp-route-Id="@item.Id" class="btn btn-danger mx-1">Dalete</a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p>No Expenses Created Yet</p>
        }
    }
</div>